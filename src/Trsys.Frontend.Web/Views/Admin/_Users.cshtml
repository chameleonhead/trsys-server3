@model Trsys.Frontend.Web.Models.Admin.UsersViewModel

<div class="list-group">
    @foreach (var item in Model.Items)
    {
        <a asp-action="UserDetails" asp-route-id="@item.Id" class="list-group-item list-group-item-action">@item.Nickname</a>
    }
</div>